build/
tools/merlin32
tools/ac
tools/acx
tools/ac-mac-*
tools/acx-mac-*
vendor/merlin32/
vendor/*.po
vendor/*.dsk
tests/snapshots/*.actual.png
.DS_Store
notes/
src/_FileInformation.txt
src/*.SYSTEM
build/FIXTURE*.po

# The launch-test programs are assembled by tests/mklaunch.sh from the .S files
# beside them. A committed binary stops matching its source the moment anybody
# edits the source, which is exactly the failure these exist to catch.
tests/RUNME
tests/RUNBIN
tests/_FileInformation.txt
tests/*_Output.txt
tests/QUITTER
src/ZFQUIT.BIN
build/QUITTEST.po
*.quitsave
src/QPATCH.SYSTEM
src/zfquitdata.S

# The web page lives with the rest of the trompingmarmots.com sites, not here
# -- the same arrangement ZipEdit uses. The screenshots it is built from are
# large and are source material for the page, not for the program.
web/
Screenshots/
build/zipfiler-site.html
